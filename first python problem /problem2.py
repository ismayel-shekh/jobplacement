# Taking two integer inputs
a, b = map(int, input().split())

# Performing operations and printing the results in the specified format
print(f"{a} + {b} = {a + b}")
print(f"{a} - {b} = {a - b}")
print(f"{a} * {b} = {a * b}")
print(f"{a} / {b} = {a / b:.2f}")
